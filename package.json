{
  "name": "alfareclame-nl",
  "version": "1.0.0",
  "private": true,
  "description": "Alfa Reclame Rotterdam — statische signage-site (Eleventy)",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "watch": "eleventy --watch",
    "ci": "npm ci && npm run build",
    "cheatsheet": "node scripts/generate-cheatsheet-pdf.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pdfkit": "^0.15.2"
  }
}
