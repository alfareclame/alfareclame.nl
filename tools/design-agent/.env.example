# 21st.dev API key — get from https://21st.dev dashboard.
# Never commit the real .env file. Already in repo .gitignore.
API_KEY_21ST=21st_sk_REPLACE_WITH_YOUR_KEY

# Optional: Anthropic API key, only needed if not using 21st.dev's managed proxy.
# ANTHROPIC_API_KEY=sk-ant-REPLACE_WITH_YOUR_KEY
